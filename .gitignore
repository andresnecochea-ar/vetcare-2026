# Caché de herramientas (wrangler) — contiene datos de cuenta, no debe subirse
backend/.wrangler/
.wrangler/
.wrangler-dist/
.wrangler-config/

# Copias locales de respaldo (git ya guarda el historial)
backups/

# Temporales de migración
backend/_*.js
backend/migrate.py

# Dependencias y cachés locales
node_modules/
.npm-cache/
dist/

# Secretos locales de Cloudflare
.dev.vars
.dev.vars.*
!.dev.vars.example
backend/.dev.vars
backend/.dev.vars.*
!backend/.dev.vars.example

# ZIPs de descarga usados solo para esta recuperación
_github/

# Backup del sistema veterinario anterior (datos reales de clientes/pacientes) — NUNCA subir a un repo público
_otros/

# Sistema operativo
.DS_Store
Thumbs.db
